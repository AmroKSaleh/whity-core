# Generated by Cargo
# will have compiled files and executables
/target/

# Generated by Tauri
# will have schema files for capabilities auto-completion
/gen/schemas

# Fetched by scripts/setup-php-runtime.ps1 — the FrankenPHP binary + its ~50
# sibling DLLs (~150MB+ uncompressed). Not committed: fetched fresh per-clone
# / per-CI-run, same as node_modules or target/, rather than bloating the repo
# with binary release artifacts.
/resources/frankenphp/

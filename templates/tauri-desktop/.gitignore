# Logs
logs
*.log
npm-debug.log*

node_modules
dist
dist-ssr
*.local

# Local build config (backend URL). Committed as .env.example instead —
# src-tauri/build.rs reads this one at compile time.
.env
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

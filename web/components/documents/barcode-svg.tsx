/**
 * Backward-compatible re-export shim: `BarcodeSvg` moved to
 * `@amroksaleh/ui/documents/barcode-svg` (WC doc-designer-ui-extraction) — it
 * had no app-specific dependencies, so it's a drop-in portable component.
 * Every existing `./barcode-svg` import keeps working unchanged.
 */
export * from '@amroksaleh/ui/documents/barcode-svg';

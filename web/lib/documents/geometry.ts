/**
 * Backward-compatible re-export shim: the canvas alignment/snapping helpers
 * moved to `@amroksaleh/ui/documents/geometry` (WC doc-designer-ui-extraction).
 * Every existing `@/lib/documents/geometry` import keeps working unchanged.
 */
export * from '@amroksaleh/ui/documents/geometry';

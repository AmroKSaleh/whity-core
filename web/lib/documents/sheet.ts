/**
 * Backward-compatible re-export shim: the N-up label-sheet layout helpers moved
 * to `@amroksaleh/ui/documents/sheet` (WC doc-designer-ui-extraction). Every
 * existing `@/lib/documents/sheet` import keeps working unchanged.
 */
export * from '@amroksaleh/ui/documents/sheet';

/** Backward-compatible re-export shim — see ./presets.ts. */
export {
  MAX_BATCH_ROWS,
  DEFAULT_SEQUENCE,
  generateSequence,
  rowsFromValues,
  rowsFromRecords,
  type SequenceConfig,
} from '@amroksaleh/features/document-designer';
